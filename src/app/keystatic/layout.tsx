/**
 * Het beheerscherm staat los van de rest van de site: geen koptekst,
 * geen voettekst, geen animaties. Keystatic tekent hier zijn eigen scherm.
 */

export default function KeystaticLayout({ children }: { children: React.ReactNode }) {
  return children
}
